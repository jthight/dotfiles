cp ~/.bash{rc,_aliases} /mnt/c/Users/jthig/
cp ~/.bash{rc,_aliases} /mnt/c/Users/jthig/Dropbox/Documents/dotfiles/
cp ~/.bash{rc,_aliases} /mnt/c/Users/jthig/OneDrive/Documents/dotfiles/
cp ~/.bash{rc,_aliases} ~/src/memories-for-tomorrow/files/
cp ~/.bash{rc,_aliases} ~/src/ansible/data/
cp ~/.bash{rc,_aliases} ~/src/secret_stuff/
# Add all files to the repository 
cd ~/src/memories-for-tomorrow/
git add .
# Commit changes with message with current date stamp
git commit -m "bashrc and bash_aliases files on `date +'%d-%m-%Y %H:%M:%S'`"
# Push changes towards GitHub
git push -u origin master
cd ~/src/secret_stuff/
# Add all files to the repository 
git add .
# Commit changes with message with current date stamp
git commit -m "bashrc and bash_aliases files on `date +'%d-%m-%Y %H:%M:%S'`"
# Push changes towards GitHub
git push -u origin main
cd ~/src/ansible/
# Add all files to the repository 
git add .
# Commit changes with message with current date stamp
git commit -m "bashrc and bash_aliases files on `date +'%d-%m-%Y %H:%M:%S'`"
# Push changes towards GitHub
git push -u origin main
