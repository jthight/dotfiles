cp ~/.tmux.conf /mnt/c/Users/jthig/
cp ~/.tmux.conf /mnt/c/Users/jthig/Dropbox/Documents/dotfiles/
cp ~/.tmux.conf /mnt/c/Users/jthig/OneDrive/Documents/dotfiles/
cp ~/.tmux.conf ~/src/memories-for-tomorrow/files/
cp ~/.tmux.conf ~/src/ansible/data/
cp ~/.tmux.conf ~/src/secret_stuff/
# Add all files to the repository 
cd ~/src/memories-for-tomorrow/
git add .
# Commit changes with message with current date stamp
git commit -m "tmux.conf files on `date +'%d-%m-%Y %H:%M:%S'`"
# Push changes towards GitHub
git push -u origin master
cd ~/src/secret_stuff/
# Add all files to the repository 
git add .
# Commit changes with message with current date stamp
git commit -m "tmux.conf files on `date +'%d-%m-%Y %H:%M:%S'`"
# Push changes towards GitHub
git push -u origin main
cd ~/src/ansible/
# Add all files to the repository 
git add .
# Commit changes with message with current date stamp
git commit -m "tmux.conf files on `date +'%d-%m-%Y %H:%M:%S'`"
# Push changes towards GitHub
git push -u origin main
